[pibrick]
piBrick ansible_host=192.168.1.99 ansible_user=mbatistela

# Use this group when running the playbook from a session on the device itself
# (e.g. `ansible-playbook site.yml --limit local`), instead of over SSH.
[local]
piBrick-local ansible_connection=local ansible_user=mbatistela
